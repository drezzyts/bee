// Echo Statement
echo "Hello, World";
echo 10 * 5;

// Expression Statement;
10 * 5;

// Variables;
mut? name = "drezzy"; // constant
mut age;
age = 18;

// Block Statement
mut blocks = 0;

{
  blocks = blocks + 1;
  
  {
    blocks = blocks + 1;
    echo blocks + " blocks";
  
  }
  blocks = blocks - 1;
  echo blocks + " blocks";
}

blocks = blocks - 1;
echo blocks + " blocks";
