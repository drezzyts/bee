// Echo Statement
echo "Hello, World";
echo 10 * 5;

// Expression Statement;
10 * 5;

// Variables;
const name = "drezzy";
mut age;
age = 18;

// Block Statement
mut blocks = 0;

{
  blocks = blocks + 1;
  
  {
    blocks = blocks + 1;
    echo blocks + " blocks";
  
  }
  blocks = blocks - 1;
  echo blocks + " blocks";
}

blocks = blocks - 1;
echo blocks + " blocks";


// If Statement

mut age = 18;

if (age >= 18) 
  echo ">= 18";
else if (age >= 16)
  echo ">= 16";
else 
  echo "< 16";

// While Statement

mut i = 0;

while (i < 5) {
  echo i;
  i = i + 1;
}

// For Statement

for (mut i = 0; i < 5; i = i + 1) {
  echo i;
}

// Function Statements
fun sum(a, b) {
  mut res = a + b;
  print(res);

  return res;
}

print(sum(11, 22));