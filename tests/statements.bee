// Echo Statement
echo "Hello, World";
echo 10 * 5;

// Expression Statement;
10 * 5;

// Variables;
mut? name = "drezzy"; // constant
mut age;
age = 18;

// Block Statement
mut blocks = 0;

{
  blocks = blocks + 1;
  
  {
    blocks = blocks + 1;
    echo blocks + " blocks";
  
  }
  blocks = blocks - 1;
  echo blocks + " blocks";
}

blocks = blocks - 1;
echo blocks + " blocks";


// If Statement

mut age = 18;

if (age >= 18) 
  echo ">= 18";
else if (age >= 16)
  echo ">= 16";
else 
  echo "< 16";